mixin alert-success(time)
  -if (messages.success)
    div(class="alert alert-success" data-time=time show-alert) #{messages.success}

mixin alert-error(time)
  -if(messages.error)
    div(class="alert alert-danger" show-alert data-time=time) #{messages.error}